extends ../article_add_layout

block newArticleInfo
    p
        input(type='text' name='question' placeholder='what is your question' maxlength="200" class="title" id="question" required)
    p.infoP
        span(id='questionInfo' class='infoSpan') 200/200
    p
        input(id='rbP' class='radioButton' type='radio' value='p' name="public" checked)
        label(for='rbP') public
        input(id='rbA' class='radioButton' type='radio' value='a' name="public")
        label(for='rbA') anonymous

block newArticleContent
    b-editor(id='editor' width="90%" height="600px" name="content" youtublog='0')

block newArticleSubmit
    button(class="articleSubmit" id='submit' name='t' onclick='postArticle(this)') ask

    script.
        function checksubmit(){
            if(title.value.length < 5){
                alert('judulnya harus lebih dari 5 alphabet');
                return false;
            }
            if(content.value.length < 5){
                alert('kontennya harus lebih dari 5 alphabet');
                return false;
            }
            return true;
        }
        question.addEventListener('keyup', function(){
            var count = 200;
            count -= question.value.length;
            questionInfo.innerHTML = count + '/200';
            if(count < 1){
                questionInfo.innerHTML = 'exceeds the limit';
            }
        });
