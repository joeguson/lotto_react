doctype html
html
    head
        title beritamus
        // what appears on the tab
        base(href="/")
        link(rel='stylesheet', href='common.css' type="text/css")
        //link(rel='stylesheet', href='common2.css' type="text/css")
        // links css common file
        link(rel='stylesheet', href='aku.css' type="text/css")
        // links css file
        link(href="https://fonts.googleapis.com/css?family=Lato&display=swap" rel="stylesheet")
        meta(charset='UTF-8')
        meta(name='viewport', content='width=device-width, initial-scale=1')
        meta(name='apple-mobile-web-app-capable', content='yes')
        // iphone에서 어플식으로 저장가능
        // Lorem ipsum
    body
    -var datemaker = function(date){
    -var tempdate = new Date(date);
    -var nowdate = new Date();
    -var year = tempdate.getFullYear();
    -var year = tempdate.getFullYear();
    -var month = tempdate.getMonth();
    -var day = tempdate.getDate();
    -var diff = nowdate - tempdate;
    -if(diff > 864000000){return month+'-'+day+'-'+year;}
    -else{
    -if(diff > 86400000){return parseInt(diff/86400000)+' hari yang lalu';}
    -else{
    -if(diff > 3600000){return parseInt(diff/3600000)+' jam yang lalu';}
    -else{
    -if(diff > 60000){return parseInt(diff/60000)+' menit yang lalu';}
    -else{return parseInt(diff/1000)+' detik yang lalu';}
    -}
    -}
    -}
    -}
        header
            a(href='/aku') Beritamus
        nav
            ul
                li(style='border-bottom: 2px solid yellow; opacity: .9')
                    a(href='/aku') Aku
                li
                    a(href='/cari') Cari
                li
                    a(href='/penobrol') Penobrol
                li
                    a(href='/tandya') Tandya
                br.clear
        section.sub-main(id='sub-main')
            //.ad Advertisement
        section.main(id='main')
            if user
                .p-stat
                    .likeStat
                        span.likeSpan
                            p.likeHeading Penobrol
                            p.likeCount #{totalp_like}
                        span.likeSpan
                            p.likeHeading Tandya
                            p.likeCount #{totalt_like}
                    .likeStat
                        span.likeSpan 
                            p.likeHeading Comment
                            p.likeCount #{totalpc_like}
                        span.likeSpan 
                            p.likeHeading Answer
                            p.likeCount #{totalta_like}
                .p-penobrol
                    p Penobrol
                    if penobrols
                        each p in penobrols
                            -var plink = 'penobrol/'+p.id
                            dl(onclick ='location.href="#{plink}"')
                                dt
                                    a(href='/penobrol/'+p.id)= p.title
                                - var date_result = datemaker(p.date) + ' / view: ' + p.p_view + ' / ' + p.com +' kommen'+' / suka: ' + p.p_like
                                dd.content-info date: #{date_result}         
                .p-tandya 
                    p Tandya
                    if tandyas
                        each t in tandyas
                            -var tlink = 'tandya/'+t.id
                            dl(onclick ='location.href="#{tlink}"')
                                dt
                                    a(href='/tandya/'+t.id)= t.question
                                - var date_result = datemaker(t.date) + ' / view: ' + t.t_view + ' / jawaban: ' + t.answer + ' / suka: ' + t.t_like
                                dd.content-info date: #{date_result}        
                a.logout(href= '/aku/logout') Logout!
            else
                form(action='aku/login' method='post')
                    input(type='text' name='u_id' placeholder='    ID' id="input_id")
                    input(type='password' name='u_pw' placeholder='    Password' id="input_pw")
                    if message
                        p.message= message
                    input(type='submit' value="Login" id="login")
                .daftar
                    span(id="hilang")
                        a Forgot
                    span(id="daftar")
                        a(href='/daftar') Sing Up
        section.footer(id='footer') 
            p contact : admin@beritamus.com
            p www.beritamus.com
    script(type="text/javascript" src="aku.js")
