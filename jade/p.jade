doctype html
html(lang="id")
    head
        title beritamus
        // what appears on the tab
        base(href="/")
        link(rel='stylesheet', href='common.css' type="text/css")
        //link(rel='stylesheet', href='common2.css' type="text/css")
        // links css common file
        link(rel="stylesheet" href="penobrol.css" type="text/css")
        // links css file
        link(href="https://fonts.googleapis.com/css?family=Lato:100,100i,300,300i,400|Pattaya&display=swap" rel="stylesheet")
        meta(charset='UTF-8')
        meta(name='viewport', content='width=device-width, initial-scale=1')    
        meta(name='apple-mobile-web-app-capable', content='yes')
        // iphone에서 어플식으로 저장가능
        // Lorem ipsum
    body
    -var datemaker = function(date){
    -var tempdate = new Date(date);
    -var nowdate = new Date();
    -var year = tempdate.getFullYear();
    -var year = tempdate.getFullYear();
    -var month = tempdate.getMonth();
    -var day = tempdate.getDate();
    -var diff = nowdate - tempdate;
    -if(diff > 864000000){return month+'-'+day+'-'+year;}
    -else{
    -if(diff > 86400000){return parseInt(diff/86400000)+' days ago';}
    -else{
    -if(diff > 3600000){return parseInt(diff/3600000)+' hours ago';}
    -else{
    -if(diff > 60000){return parseInt(diff/60000)+' minutes ago';}
    -else{return parseInt(diff/1000)+' seconds ago';}
    -}
    -}
    -}
    -}
    -var anonimmaker = function(username){
    -var idChanger = '';
    -var idLength = (username).length;
    -idChanger = username.substr(0,3) + '****';
    -return idChanger;
    -}
        header
            a(href='/penobrol') Beritamus
        nav
            ul
                li
                    a(href='/aku') Aku
                li
                    a(href='/cari') Cari
                li(style='border-bottom: 2px solid yellow; opacity: .9')
                    a(href='/penobrol') Penobrol
                li
                    a(href='/tandya') Tandya
                br.clear
        section.sub-main(id='sub-main')
            //.ad Advertisement
        section.main(id='main')
            .login
                if(u_id)
                    a.addPenobrol(href='/penobrol/add') +
                else
                    a.addPenobrol(href='/aku') + 
            .p-terbaru
                .p-title Penobrol Terbaru
                ul
                    each v in dateTopics
                        li
                            -var plink = 'penobrol/'+v.id
                            dl.p-dl(onclick ='location.href="#{plink}"')
                                dt
                                    a(href='/penobrol/'+v.id)= v.title
                                dd.ddcontent= v.content
                                if v.public == 'a'
                                    - var date_result = 'by '+anonimmaker(v.author)+' / ' + datemaker(v.date) + ' / ' + v.p_view + ' views' + ' / ' + v.com+' komentar'
                                else
                                    - var date_result = 'by '+v.author+' / ' + datemaker(v.date) + ' / ' + v.p_view + ' views' + ' / ' + v.com+' komentar'
                                dd #{date_result}        
            .p-terkenal
                .p-title Penobrol Terkenal
                ul
                    each w in scoreTopics
                        li
                            -var plink = 'penobrol/'+w.id
                            dl.p-dl(onclick ='location.href="#{plink}"')
                                dt
                                    a(href='/penobrol/'+w.id)= w.title
                                dd.ddcontent= w.content
                                if w.public == 'a'
                                    - var date_result = 'by '+anonimmaker(w.author)+' / ' + datemaker(w.date) + ' / ' + w.p_view + ' views' + ' / ' + w.com+' komentar'
                                else
                                    - var date_result = 'by '+w.author+' / ' + datemaker(w.date) + ' / ' + w.p_view + ' views' + ' / ' + w.com+' komentar'
                                dd #{date_result}   
        section.footer(id='footer') 
            p contact : admin@beritamus.com
            p www.beritamus.com
    //script(type="text/javascript" src="prank.js")